# Base URL of the Go API. Defaults to the local dev API (see api/client.ts)
# if this file isn't copied to .env — only needed when the API runs
# somewhere other than http://localhost:8081.
VITE_API_BASE_URL=http://localhost:8081
